# TLS Diagnostics
# Purpose: Inspect TLS version, cipher, and certificate verification behavior

curl -s -o /dev/null -w '
TLS Diagnostics
---------------
    TLS version:       %{ssl_version}
    Cipher suite:      %{ssl_cipher}
    Cert verify:       %{ssl_verify_result}
    Connect time:      %{time_connect}s
    TLS handshake:     %{time_appconnect}s
'
